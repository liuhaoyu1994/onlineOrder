<%= @restaurant.name %>
<%= @restaurant.address %>
<%= image_tag(@restaurant.photo_url) %>
<br>
<% @restaurant.categories.each do |cat| %>
    <%= link_to cat.name, cat %>
    <br>
<% end %>
New Category
<%= render partial: 'restaurants/category_form'%>
